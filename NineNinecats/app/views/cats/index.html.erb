<h1>All Cats</h1>

<a href="<%= new_cat_url %>">Create a new Cat</a>

<ul>
    <% @cats.each do |cat| %>
        <li><%= cat.name %></li>
        <a href="<%= cat_url(cat) %>">Show the Cat</a>
    <% end %>
</ul>